<?xml version="1.0"?>

<ruleset name="Subsite">
    <description>Custom ruleset for subsite usage.</description>

    <!-- Added in order to test build, all the errors should be fixed. -->
    <exclude-pattern>**</exclude-pattern>
    
</ruleset>